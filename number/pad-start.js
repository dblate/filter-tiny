/**
 * @file pad-start
 * @author yuhui06
 * @date 2018/11/14
 */

/**
 * 使用指定填充符(串)给数值添加前缀，使其达到指定长度
 *
 * @param {number} number 待填充的数字
 * @param {number} targetLength 指定长度
 * @param {string} padString 用于填充的字符串
 * @return {string} 填充后的字符串
 * @example
 *    padStart(1, 2, '0') => '01'
 *    padStart(12, 2, '0') => '12'
 */
function padStart(number, targetLength, padString) {

}

module.exports = padStart;
